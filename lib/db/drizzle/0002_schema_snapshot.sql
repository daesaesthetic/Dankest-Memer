-- This records the current schema for drizzle-kit after the hand-authored
-- baseline and hardening migrations. The schema is already established by
-- migrations 0000 and 0001, so this migration intentionally makes no change.
SELECT 1;